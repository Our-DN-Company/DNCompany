<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.dncompany.mapper.zip.ZipMapper">
    <insert id="insertBoard" parameterType="ZipWriteDTO">
        <selectKey resultType="long" keyProperty="zipId" order="BEFORE">
            SELECT SEQ_ZIP_ID.nextval FROM DAUL
        </selectKey>
        INSERT INTO ZIP_BOARD(ZIP_ID, USER_ID, ZIP_TITLE, ZIP_CONTENT)
        VALUES (#{zipId}, #{userId}, #{zipTitle}, #{zipContent})
    </insert>
    
    <select id="selectAllBoards" resultType="Zip"
</mapper>