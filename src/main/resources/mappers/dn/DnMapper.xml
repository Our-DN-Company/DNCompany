<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.dncompany.mapper.dn.DnMapper">
    <insert id="insertDnBoard" parameterType="DnBoardDTO">
        <selectKey resultType="Long" keyProperty="dnId" order="BEFORE">
            SELECT SEQ_DN_BOARD.nextval FROM DUAL
        </selectKey>
        INSERT INTO DN_BOARD (DN_ID, USERS_ID, DN_PET_CATEGORY, DN_TITLE, DN_CONTENT)
        VALUES (#{dnId}, #{usersId}, #{dnPetCategory},#{dnTitle}, #{dnContent});
    </insert>

    <insert id="insertProduct" parameterType="ProductDTO">
        <selectKey resultType="Long" keyProperty="productId" order="BEFORE">
            SELECT SEQ_PRODUCT FROM DUAL
        </selectKey>
        INSERT INTO PRODUCT (PRODUCT_ID, PRODUCT_PRICE, PRODUCT_CATEGORY)
        VALUES (#{productId}, #{productPrice}, #{productCategory});
    </insert>

</mapper>